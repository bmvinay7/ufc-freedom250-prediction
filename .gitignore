# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/

# OS
.DS_Store

# Generated by the pipeline (recreate with ./run_all.sh)
data/feature_matrix.csv
data/fighter_state.json
models/*.pkl
models/meta.json

# Keep committed (so the repo is browsable + the live demo works without running):
#   data/raw/*           source data
#   outputs/predictions.json   final predictions
#   docs/data.js         dashboard data
